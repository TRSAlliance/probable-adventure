{
  "name": "ghostshift-tracker",
  "version": "2.4.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:vercel": "npm run build && vercel --prod",
    "deploy:netlify": "npm run build && netlify deploy --prod",
    "sentinel": "node src/sentinel/simulator.js",
    "reprocess": "node src/sentinel/simulator.js --reprocess"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
